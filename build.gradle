buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.13.1' // match the version used by FTC SDK
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
